name: First Workflow

on:
  push:

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a String
        run: echo "Hello World 121200000000"

      - name: Multiline Commands
        run: |
          node --version
          npm --version
