name: Workflow 2.0

on:
  push:

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a String
        run: echo "Hello World 11111om22222"

      - name: Multiline Commands
        run: |
          node --version
          npm --version
